<Project>
	<PropertyGroup>
		<!-- Single source of truth for the version: the NuGet package version, every assembly version, and
         the OpenTelemetry instrumentation-scope version (SocigyDbInstrumentation) all derive from this.
         Bump it here on each release. -->
		<Version>0.3.2</Version>
	</PropertyGroup>
</Project>
